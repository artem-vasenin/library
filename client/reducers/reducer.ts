// import {IGlobalState} from '../models';

const initialState = {
    list: [],
    findedItems: [],
    searchValue: '',
    isLoading: false,
    isError: false
  };

export default function reducer(state: any = initialState, action: any) {
	return false;
}